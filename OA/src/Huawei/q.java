package Huawei;

public class q {
    /**
     * SELECT t1.name, t2.total_score
     * FROM T t1
     * WHERE t1.name = t2.name
     * ORDER BY t2.total_score DESC
     * Having 
     * LIMIT 10;
     */
}
